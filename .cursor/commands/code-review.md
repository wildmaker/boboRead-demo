1. Retrieve the PR metadata and file changes for the given PR.
2. Run the command `"git-pr-summarize"` to generate a PR summary.
3. After the PR summary is generated, run the command `"report-it-to-me"` to generate a code review report using the PR summary markdown file.
4. Open the code review report in the browser inside the Cursor IDE.
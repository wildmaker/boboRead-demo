# 🧭 生成 PR 汇报 Markdown（中文，PPT 格式）

**说明**  
面向指定 GitHub PR，自动生成一份面向 CTO 的中文汇报 Markdown，适合转换为 reveal.js PPT。重点突出核心逻辑、整体设计，使用丰富的可视化图表。

## 命令（Cursor）
```yaml
name: "生成 PR 汇报 Markdown（中文，PPT 格式）"
description: "从指定 PR 生成面向 CTO 的中文汇报 Markdown（适合 reveal.js PPT，可视化+核心逻辑）"
command:
  steps:
    - name: 获取 PR 元信息与文件变更
      run: >
        gh pr view {{input:pr}} --json
        files,title,number,headRefName,baseRefName,author,url,body,commits,repository
        --jq '.' > /tmp/pr.json
    - name: 读取 diff（统一 0 行上下文，便于定位）
      run: >
        gh pr diff {{input:pr}} --unified=0 > /tmp/pr.diff || true
    - name: 生成汇报 Markdown（中文）
      run: >
        ai.comment "
        你是一名 PR 提交者，正在向 CTO 汇报你的 PR。请阅读以下 PR 数据与 diff，
        输出一份**中文**、结构化、适合 PPT 展示的 Markdown 文档。
        
        **角色定位**：你是 PR 提交者，向 CTO 汇报。采用先概括后具体的讲述方式。**CTO 决策优先**。
        
        **核心原则**：
        1. **严禁臆测**：所有关键结论必须基于 PR 数据和 diff，不得臆测。每个关键结论后必须附"证据"链接（文件链接、提交链接、检查状态链接等）。如果缺少数据，必须标注【缺数据】。
        2. **所有内容必须使用中文**
        3. **每个章节之间使用 `---` 分隔符**（reveal.js 幻灯片分隔符）
        4. **每页 ≤ 7 要点**：严格控制内容密度，适合单页 PPT 展示
        5. **代码文件引用必须使用 GitHub 可点击链接格式**：\`[文件名](仓库URL/blob/分支/文件路径#L起始行-L结束行)\`
        6. **优先使用可视化图表**：Mermaid 流程图、时序图、类图、组件图、状态图等
        7. **每个复杂流程至少 1 个图**，图后必须附 2～3 句"给 CTO 的解读"
        
        **内容结构**（每个章节用 `---` 分隔）：
        
        ## 1. 封面页
        - PR 标题、编号、链接
        - 分支信息（base → head）
        - 提交者
        - 一句话核心价值
        
        ---
        
        ## 2. 我需要你拍板的事项（CTO 决策优先）
        - **需确认**：需要 CTO 确认的事项，每条 1 句话 + 证据链接
        - **需取舍**：需要 CTO 做取舍决策的事项，每条 1 句话 + 证据链接
        - **需资源**：需要 CTO 协调资源的事项，每条 1 句话 + 证据链接
        - 如果没有需要拍板的事项，标注"无需要拍板事项"
        - 每条必须附证据：文件链接、提交链接、检查状态链接等，缺数据时标注【缺数据】
        - 限制在 3-5 个要点
        
        ---
        
        ## 3. 核心价值与目标
        - 这个 PR 要解决什么问题？（附证据链接）
        - 业务价值是什么？（附证据链接）
        - 技术目标是什么？（附证据链接）
        - 限制在 3-5 个要点，每个要点后附证据，缺数据时标注【缺数据】
        
        ---
        
        ## 4. 整体架构设计
        - 用 Mermaid 组件图或架构图展示整体设计
        - 说明核心模块及其职责（附文件链接证据）
        - 模块间的交互关系
        - 如有新引入的技术栈，简要说明选择理由（附证据链接）
        - **图后 2～3 句"给 CTO 的解读"**
        
        ---
        
        ## 5. 核心实现逻辑
        - 用 Mermaid 时序图展示关键流程
        - 用流程图展示核心算法或业务逻辑
        - 说明关键设计决策（附文件链接证据）
        - **图后 2～3 句"给 CTO 的解读"**
        - 每个关键结论后附证据链接，缺数据时标注【缺数据】
        
        ---
        
        ## 6. 关键代码模块
        - 按模块/目录组织，每个模块 3-5 个要点
        - 每个要点包含：模块职责、关键文件链接（GitHub 链接格式）、核心改动说明
        - 文件链接格式：\`[文件名](仓库URL/blob/分支/文件路径#L起始行-L结束行)\`
        - 避免逐行 diff，聚焦语义层面的变化
        - 每个改动说明后附证据链接
        
        ---
        
        ## 7. 数据流与状态变化
        - 如有数据模型变化，用 Mermaid ER 图或类图展示（附文件链接证据）
        - 如有状态机，用 Mermaid 状态图展示（附文件链接证据）
        - 说明数据流转路径
        - **图后 2～3 句"给 CTO 的解读"**
        - 缺数据时标注【缺数据】
        
        ---
        
        ## 8. 接口与契约
        - API 变更（如有）：用 Mermaid 序列图展示调用流程（附文件链接证据）
        - 数据库变更（如有）：简要说明 DDL 变化（附文件链接证据）
        - 外部依赖变更（如有）：附依赖文件链接证据
        - 兼容性说明（附测试或文档证据）
        - **图后 2～3 句"给 CTO 的解读"**
        - 缺数据时标注【缺数据】
        
        ---
        
        ## 9. 测试策略
        - 测试覆盖范围（附测试文件链接证据）
        - 关键测试用例（3-5 个，每个附测试文件链接）
        - 验证方法（附 CI/CD 或测试报告链接）
        - 缺数据时标注【缺数据】
        
        ---
        
        ## 10. 风险与应对
        - 技术风险（2-3 个要点，每个附相关文件或提交链接证据）
        - 性能影响（如有，附性能测试或监控数据链接）
        - 回滚方案（附回滚脚本或文档链接）
        - 监控与告警建议（附监控配置或告警规则链接）
        - 缺数据时标注【缺数据】
        
        ---
        
        ## 11. 总结与后续
        - 核心成果总结（附关键文件或提交链接证据）
        - 后续计划（如有，附相关 issue 或文档链接）
        
        **可视化要求**：
        - 优先使用 Mermaid 图表：flowchart、sequenceDiagram、classDiagram、stateDiagram、erDiagram、graph 等
        - **每个复杂流程至少 1 个图**
        - 图表要清晰、简洁，适合 PPT 展示
        - **每个图表后必须附 2～3 句"给 CTO 的解读"**，说明图表的关键信息和决策点
        
        **内容密度控制（讲述友好 & 幻灯片化）**：
        - **每页 ≤ 7 要点**：严格控制，适合单页 PPT 展示
        - 每个章节控制在 1 页 PPT 的内容量
        - 每个要点 1-2 句话
        - 避免大段文字，多用图表和列表
        
        **输入数据**：
        - PR JSON:
        \`\`\`json
        $(cat /tmp/pr.json)
        \`\`\`
        - PR DIFF (unified=0):
        \`\`\`
        $(cat /tmp/pr.diff)
        \`\`\`
        
        **输出格式**：
        - 只输出 Markdown 内容，不要多余解释
        - 确保所有章节之间都有 `---` 分隔符
        - 确保所有代码文件链接都是可点击的 GitHub 链接格式
        - 确保所有 Mermaid 代码块格式正确
        - **严禁臆测**：所有关键结论必须附证据链接，缺数据时标注【缺数据】
        - 每个关键结论后必须附证据：文件链接、提交链接、检查状态链接等
        "
    - name: 提取 PR 信息用于文件命名
      run: |
        OWNER=$(cat /tmp/pr.json | jq -r '.repository.owner.login')
        REPO=$(cat /tmp/pr.json | jq -r '.repository.name')
        NUM=$(cat /tmp/pr.json | jq -r '.number')
        echo "pr-${OWNER}-${REPO}-${NUM}" > /tmp/pr-filename.txt
    - name: 保存到仓库 docs/reviews/
      run: |
        mkdir -p docs/reviews
        FILENAME=$(cat /tmp/pr-filename.txt)
        echo '{{previous_output}}' > docs/reviews/${FILENAME}.md
        echo "已保存到: docs/reviews/${FILENAME}.md"
    - name: 打开预览
      run: |
        FILENAME=$(cat /tmp/pr-filename.txt)
        open docs/reviews/${FILENAME}.md
inputs:
  - name: pr
    description: "GitHub PR 编号、URL 或分支（如 42 或 owner/repo#42 或 head 分支名）"
    type: string
